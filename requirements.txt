# --- Web 框架 ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # 处理文件上传

# --- LangChain 全家桶 (新版架构) ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0        # DeepSeek & Azure 依赖
langchain-text-splitters>=0.3.0
langchain-huggingface>=0.1.0   # 【新增】本地 Embedding 模型依赖
langgraph>=0.2.0               # Agent 编排

# --- 大模型客户端 ---
openai>=1.30.0                 # 【关键】支持 Azure 和 DeepSeek 原生调用

# --- 网络与异步 ---
httpx>=0.27.0
aiohttp>=3.9.0
requests>=2.30.0               # 【新增】LLM Service HTTP 请求依赖

# --- RAG 与 向量库 ---
faiss-cpu>=1.8.0               # 向量数据库
sentence-transformers>=3.0.0   # 本地模型
beautifulsoup4>=4.12.0         # 网页解析

# --- 数据库与工具 ---
sqlalchemy>=2.0.0
aiosqlite>=0.20.0              # 异步 SQLite
python-dotenv>=1.0.1           # 读取 .env 配置
tiktoken>=0.7.0                # Token 计算
pydantic>=2.8.0                # 数据校验
pandas>=2.0.0                 # 【新增】Excel/CSV 处理
openpyxl>=3.1.0               # 【新增】Excel 文件支持

# --- PDF处理 (Marker) ---
marker-pdf==0.3.2              # Marker PDF处理库 (OCR + 文本提取)
torch>=2.0.0                   # PyTorch (Marker依赖)
transformers>=4.30.0           # HuggingFace Transformers
pillow>=10.0.0                 # 图像处理
pyyaml>=6.0                    # 配置文件解析