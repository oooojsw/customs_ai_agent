[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    slow: long-running tests that are not part of the default local suite
    live_model: tests that call a real LLM or live agent loop
    integration: tests that require a running service, browser, or external toolchain
